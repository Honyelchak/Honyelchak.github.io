import{_ as l}from"./plugin-vue_export-helper-x3n3nnut.js";import{r as s,o as d,c as r,a as e,b as a,e as c,f as i}from"./app-2Xbp2Heq.js";const t="/assets/image-20230315222724383-146JyVgX.png",p="/assets/image-20230315225846602-4dYQX1mp.png",n="/assets/image-20200207141345626-5Pau9ZRD.png",u="/assets/image-20230316145811418-18PM9p1U.png",h="/assets/image-20200208110718819-PVsxjRA2.png",g="/assets/image-20230316210626456-bs717boU.png",m="/assets/image-20230316200916584-S0V84JQl.png",b="/assets/image-20200229174914201-7MC0E4rn.png",k="/assets/image-20200229175056259-o0xByFnK.png",x="/assets/image-20200229175259716-LlHPIDE8.png",f="/assets/image-20200229175535908-8eysIeNp.png",_="/assets/image-20230329141850272-LdX4Vz1Q.png",v="/assets/image-20200308164036307-QSTWdzKP.png",y="/assets/image-20200308161124874-lwEkbTNN.png",L="/assets/image-20200308163355427-Mt3Js64A.png",E="/assets/image-20200308171239108-rj81n9fK.png",M="/assets/image-20200308171629578-Ve8xHRGu.png",I="/assets/image-20200308171911959-D4pFGje0.png",S="/assets/image-20200308171949464-MhUk_R_n.png",R="/assets/image-20200308172143963-EimvvGCC.png",w="/assets/image-20200308172143963-EimvvGCC.png",A="/assets/image-20200308172235874-aov6Jne_.png",q={},C=i(`<h1 id="一、字符集" tabindex="-1"><a class="header-anchor" href="#一、字符集" aria-hidden="true">#</a> 一、字符集</h1><h2 id="重要的字符集" tabindex="-1"><a class="header-anchor" href="#重要的字符集" aria-hidden="true">#</a> 重要的字符集</h2><ul><li><code>ASCII</code>字符集</li></ul><p>共收录128个字符，<strong>包括空格、标点符号、数字、大小写字母和一些不可见字符。由于总共才128个字符</strong>，所以可以使用1个字节来进行编码，我们看一些字符的编码方式：</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>&#39;L&#39; -&gt;  01001100（十六进制：0x4C，十进制：76）
&#39;M&#39; -&gt;  01001101（十六进制：0x4D，十进制：77）

</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><ul><li><p><code>ISO 8859-1</code>字符集</p><p>共收录<strong>256</strong>个字符，是在<code>ASCII</code>字符集的基础上又扩充了<strong>128个西欧常用字符</strong>(包括德法两国的字母)，也可以使用1个字节来进行编码。这个字符集也有一个别名**<code>latin1</code>**。</p></li></ul><ul><li><code>GB2312</code>字符集 <ul><li>收录了汉字以及拉丁字母、希腊字母、日文平假名及片假名字母、俄语西里尔字母。其中收录汉字6763个，其他文字符号682个。同时这种字符集又兼容<code>ASCII</code>字符集，所以在编码方式上显得有些奇怪：</li><li>如果该字符在<code>ASCII</code>字符集中，则采用1字节编码，否则采用2字节编码。</li></ul></li></ul><blockquote><p>这种表示一个字符需要的字节数可能不同的编码方式称为<code>变长编码方式</code>。比方说字符串<code>&#39;爱u&#39;</code>，其中<code>&#39;爱&#39;</code>需要用2个字节进行编码，编码后的十六进制表示为<code>0xCED2</code>，<code>&#39;u&#39;</code>需要用1个字节进行编码，编码后的十六进制表示为<code>0x75</code>，所以拼合起来就是<code>0xCED275</code>。</p></blockquote><blockquote><p>小贴士： 我们怎么区分某个字节代表一个单独的字符还是代表某个字符的一部分呢？别忘了 ASCII字符集只收录128个字符，使用0～127就可以表示全部字符，所以如果某个字节是在0～127之内的，就意味着一个字节代表一个单独的字符，否则就是两个字节代表一个单独的字符。</p></blockquote><ul><li><p><code>GBK</code>字符集</p><p><code>GBK</code>字符集只是在收录字符范围上对<code>GB2312</code>字符集作了扩充，编码方式上兼容<code>GB2312</code>。</p></li><li><p><code>utf8</code>字符集</p><p><strong>收录地球上能想到的所有字符</strong>，而且还在不断扩充。这种字符集兼容<code>ASCII</code>字符集，采用变长编码方式，编码一个字符需要使用1～4个字节，比方说这样：</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>&#39;L&#39; -&gt;  01001100（十六进制：0x4C）
&#39;啊&#39; -&gt;  111001011001010110001010（十六进制：0xE5958A）

</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><blockquote><p>小贴士： 其实准确的说，utf8只是Unicode字符集的一种编码方案，Unicode字符集可以采用utf8、utf16、utf32这几种编码方案，utf8使用1～4个字节编码一个字符，<code>utf16使用2个或4个字节编码一个字符</code>，utf32使用4个字节编码一个字符。更详细的Unicode和其编码方案的知识不是本书的重点，大家上网查查哈～ MySQL中并不区分字符集和编码方案的概念，所以后边唠叨的时候把utf8、utf16、utf32都当作一种字符集对待。</p><ul><li>对于单字节的 ASCII 字符（U+0000 至 U+007F），UTF-8 使用一个字节表示，最高位设置为 0。</li><li>对于多字节的 Unicode 字符，UTF-8 使用多个字节表示，其中每个字节的最高位都设置为 1，并且紧随其后的一个比特位设置为 0。</li></ul><p>根据这个规则，UTF-8 可以区分出一个字符是用一个编码还是用多个编码。具体来说：</p><ul><li>如果一个字节的最高位是 0，那么它表示的是一个单字节的 ASCII 字符。</li><li>如果一个字节的最高位是 1，而紧随其后的比特位是 0，那么它表示的是多字节字符的第一个字节。</li><li>如果一个字节的最高位是 1，并且紧随其后的比特位也是 1，那么它表示的是多字节字符的后续字节。</li></ul></blockquote></li></ul><p>​</p><h3 id="mysql中支持的字符集和排序规则" tabindex="-1"><a class="header-anchor" href="#mysql中支持的字符集和排序规则" aria-hidden="true">#</a> MySQL中支持的字符集和排序规则</h3><p>我们上边说<code>utf8</code>字符集表示一个字符需要使用1～4个字节，但是我们常用的一些字符使用1～3个字节就可以表示了。而在<code>MySQL</code>中字符集表示一个字符所用最大字节长度在某些方面会影响系统的存储和性能，所以设计<code>MySQL</code>的大叔偷偷的定义了两个概念：</p><ul><li><code>utf8mb3</code>：阉割过的<code>utf8</code>字符集，只使用1～3个字节表示字符。</li><li><code>utf8mb4</code>：正宗的<code>utf8</code>字符集，使用1～4个字节表示字符。</li></ul><p>有一点需要大家十分的注意，在<code>MySQL</code>中<code>utf8</code>是<code>utf8mb3</code>的别名，所以之后<mark>在<code>MySQL</code>中提到<code>utf8</code>就意味着使用1~3个字节来表示一个字符</mark>，如果大家有使用4字节编码一个字符的情况，比如存储一些emoji表情啥的，那请使用<code>utf8mb4</code>。</p><blockquote><p>引申:</p><ol><li>emoij表情需要用4个表情来存储.</li><li>在MySQL8之后, utf8mb3被废弃</li></ol></blockquote><h1 id="二、数据存储" tabindex="-1"><a class="header-anchor" href="#二、数据存储" aria-hidden="true">#</a> 二、数据存储</h1><p><strong>真实数据在不同 存储引擎中存放的格式⼀般是不同的</strong></p><h3 id="innodb记录存储结构" tabindex="-1"><a class="header-anchor" href="#innodb记录存储结构" aria-hidden="true">#</a> InnoDB记录存储结构</h3><ul><li>将一个表的数据存储在磁盘的存储引擎上，所以关机数据也不会丢失。</li></ul><h4 id="数据是如何读出来的" tabindex="-1"><a class="header-anchor" href="#数据是如何读出来的" aria-hidden="true">#</a> 数据是如何读出来的？</h4><p>数据并不是一条一条的从磁盘上读出来的？</p><p><code>InnoDB</code>采取的方式是：<strong>将数据划分为若干个页，以页作为磁盘和内存之间交互的基本单位，InnoDB中页的大小一般为 <em><strong>16</strong></em> KB。也就是在一般情况下，一次最少从磁盘中读取16KB的内容到内存中，一次最少把内存中的16KB内容刷新到磁盘中。</strong></p><ol><li><p>页是<code>MySQL</code>中磁盘和内存交互的基本单位，也是<code>MySQL</code>是管理存储空间的基本单位。</p></li><li><p>指定和修改行格式的语法如下：</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>CREATE TABLE 表名 (列的信息) ROW_FORMAT=行格式名称

ALTER TABLE 表名 ROW_FORMAT=行格式名称

</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div></li><li><p>一个页一般是<code>16KB</code>，当记录中的数据太多，当前页放不下的时候，会把多余的数据存储到其他页中，这种现象称为<code>行溢出</code>。</p></li></ol><h4 id="innodb行格式" tabindex="-1"><a class="header-anchor" href="#innodb行格式" aria-hidden="true">#</a> InnoDB行格式</h4><p>记录在磁盘上的存放方式也被称为<code>行格式</code>或者<code>记录格式</code>。</p><p>4种不同类型的<code>行格式</code>，分别是<code>Compact</code>、<code>Redundant</code>(5.0之前)、<code>Dynamic</code>和<code>Compressed</code>行格式。</p><p>一条完整的记录其实可以被分为**<code>记录的额外信息</code><strong>和</strong><code>记录的真实数据</code>**两大部分。</p><h4 id="四种行格式" tabindex="-1"><a class="header-anchor" href="#四种行格式" aria-hidden="true">#</a> 四种行格式</h4><p><code>InnoDB</code>目前定义了4中行格式</p><ul><li><p>COMPACT行格式</p><p>具体组成如图：</p><figure><img src="`+t+'" alt="image-20230315222724383" tabindex="0" loading="lazy"><figcaption>image-20230315222724383</figcaption></figure></li><li><p>Redundant行格式</p><p>具体组成如图：</p><figure><img src="'+p+'" alt="image-20230315225846602" tabindex="0" loading="lazy"><figcaption>image-20230315225846602</figcaption></figure></li><li><p>Dynamic和Compressed行格式</p><p>这两种行格式类似于<code>COMPACT行格式</code>，只不过在处理行溢出数据时有点儿分歧，它们不会在记录的真实数据处存储字符串的前768个字节，而是把所有的字节都存储到其他页面中，只在记录的真实数据处存储其他页面的地址。</p><p>另外，<code>Compressed</code>行格式会采用压缩算法对页面进行压缩。</p></li></ul><hr><h4 id="compact行格式-记录的额外信息" tabindex="-1"><a class="header-anchor" href="#compact行格式-记录的额外信息" aria-hidden="true">#</a> Compact行格式-记录的额外信息</h4><p>这部分信息是服务器为了描述这条记录而不得不额外添加的一些信息，这些额外信息分为3类，分别是<code>变长字段长度列表</code>、<code>NULL值列表</code>和<code>记录头信息</code>。</p><h5 id="_1-变长字段长度列表" tabindex="-1"><a class="header-anchor" href="#_1-变长字段长度列表" aria-hidden="true">#</a> <code>1. 变长字段长度列表</code></h5><p>把所有变⻓字段的真实数据占⽤的<strong>字节⻓度</strong>都存放在记录的开头部位，从⽽形成⼀个变⻓字段⻓度列表，各变⻓字段数据占⽤的字节数<strong>按照列的顺序<u>逆序</u>存放</strong>.</p><p>另外,</p><ol><li>该列表中只存储值为<strong>非null</strong>的列内容占用的长度.</li><li>如果该条记录没有变长字段,那就没有该列表</li></ol><blockquote><p>如何确定使用一个字节还是两个字节来表示真正字符串占用的字节数?</p><p>首先定义一下 <code>W</code> <code>M</code> <code>L</code>各自的数值</p><ol><li>假设某个字符集中表示⼀个字符最多需要使⽤的字节数为W， 也就是使⽤SHOW CHARSET语句的结果中的Maxlen列，⽐⽅ 说utf8字符集中的W就是3，gbk字符集中的W就是2，ascii 字符集中的W就是1。</li><li>对于变⻓类型VARCHAR(M)来说，这种类型表示能存储最多M 个字符（注意是字符不是字节），所以这个类型能表示的字符串最多占⽤的字节数就是<strong>M×W</strong>。</li><li>假设它实际存储的字符串占⽤的字节数是L。</li></ol><p>如果M×W &lt;= 255，那么使⽤1个字节来表示真正字符串占⽤ 的字节数。</p><p>如果M×W &gt; 255，则分为两种情况：</p><ol><li>如果L &lt;= 127，则⽤1个字节来表示真正字符串占⽤的 字节数。</li><li>如果L &gt; 127，则⽤2个字节来表示真正字符串占⽤的字 节数。</li></ol><p><strong>结论:</strong> InnoDB在<strong>读取记录的变长字段长度列表</strong>时会先查看表结构, 如果M*W &lt;= 255 直接使用1个字节读取,否则查看该字节的第一位是否为0,如果是0, 说明长度是单字节,否则是双字节</p></blockquote><h5 id="_2-null值列表" tabindex="-1"><a class="header-anchor" href="#_2-null值列表" aria-hidden="true">#</a> <code>2. null值列表</code></h5><p>用整数个字节按照列的顺序逆序存放null值.</p><blockquote><ol><li>如果没有值为null的列,那就不会有这个列表</li><li>如果该列为主键/不为空的键,那么他不会纳入统计</li></ol></blockquote><h5 id="_3-记录头信息" tabindex="-1"><a class="header-anchor" href="#_3-记录头信息" aria-hidden="true">#</a> <code>3. 记录头信息</code></h5><p>它是由固定的5个字节组成, 包括是否被删除\\下一条记录的指针等等</p><h4 id="compact行格式-记录的真实数据" tabindex="-1"><a class="header-anchor" href="#compact行格式-记录的真实数据" aria-hidden="true">#</a> Compact行格式-记录的真实数据</h4><p><code>MySQL</code>会为每个记录默认的添加一些列（也称为<code>隐藏列</code>），具体的列如下：</p><figure><img src="'+n+'" alt="image-20200207141345626" tabindex="0" loading="lazy"><figcaption>image-20200207141345626</figcaption></figure><ol><li>如果用户没有定义主键, 则会选取一个unique键作为主键,如果没有unique键,就添加隐藏列row_id作为主键.</li></ol><blockquote><p>小贴士： 实际上这几个列的真正名称其实是：DB_ROW_ID、DB_TRX_ID、DB_ROLL_PTR，我们为了美观才写成了row_id、transaction_id和roll_pointer。</p></blockquote><h4 id="redundant行格式" tabindex="-1"><a class="header-anchor" href="#redundant行格式" aria-hidden="true">#</a> Redundant行格式</h4><h5 id="_1-字段长度偏移列表" tabindex="-1"><a class="header-anchor" href="#_1-字段长度偏移列表" aria-hidden="true">#</a> <code>1.字段长度偏移列表</code></h5><p>将该条记录中<strong>所有列的长度信息</strong>按照<strong>逆序</strong>存储到字段长度偏移列表中。</p><p>通过相邻两个数值的差值计算出各列值的长度。</p><h5 id="_2-记录头信息" tabindex="-1"><a class="header-anchor" href="#_2-记录头信息" aria-hidden="true">#</a> <code>2.记录头信息</code></h5><p>Redundant⾏格式的记录头信息占⽤6字节，48个⼆进制位</p><p><code>3.对于null值的处理</code></p><p>Redundant⾏格式并没有NULL值列表</p><ul><li>如果该存储NULL值的字段是变⻓数据类型的，则在字段 ⻓度偏移列表中记录即可，并不占⽤记录的真实数据部 分。</li><li>如果该存储NULL值的字段是CHAR(M)数据类型的，则将 占⽤记录的真实数据部分，并把该字段对应的数据使 ⽤0x00字节填充。</li></ul><h3 id="行溢出数据" tabindex="-1"><a class="header-anchor" href="#行溢出数据" aria-hidden="true">#</a> 行溢出数据</h3><p>MySQL对⼀条记录<strong>占⽤的最⼤存储空间</strong>是有限制的，除了BLOB或者TEXT类型的列之外，其他所有的列（<strong>不包括</strong> 隐藏列和记录头信息）<strong>占⽤的字节⻓度加起来不能超过<code>65535</code>个字节。</strong></p><ol><li><p>在Compact格式中, 如果使用ASCII字符集的话：</p><ol><li>如果该VARCHAR类型的列没有NOT NULL属性，那最多只能存储 <code>65532</code>个字节的数据，因为真实数据的⻓度可能占⽤2个字节，NULL 值标识需要占⽤1个字节</li><li>如果VARCHAR类型的列有NOT NULL属性，那最多只能存储<code>65533</code>个 字节的数据，因为真实数据的⻓度可能占⽤2个字节，不需要NULL值 标识.</li></ol></li><li><p>如果VARCHAR(M)类型的列使⽤的不是ascii字符集，<strong>那M的最⼤取值取决于该字符集表示⼀个字符最多需要的字节数</strong>。在列的值允许为NULL的情况下，gbk字符集表示⼀ 个字符最多需要2个字符，那在该字符集下，M的最⼤取值就 是32766（也就是：65532/2），也就是说最多能存储32766个字 符；utf8字符集表示⼀个字符最多需要3个字符，那在该字符集 下，M的最⼤取值就是21844，就是说最多能存储21844（也就是： 65532/3）个字符。</p><blockquote><p>上述所⾔在列的值允许为NULL的情况下，gbk字符集下M的最⼤取值 就是32766，utf8字符集下M的最⼤取值就是21844，这都是在表 中只有⼀个字段的情况下说的，⼀定要记住⼀个⾏中的所有列（不 包括隐藏列和记录头信息）占⽤的字节⻓度加起来不能超过65535 个字节</p></blockquote></li></ol><p>MySQL中磁盘和内存交互的基本单位是⻚，也就是说MySQL是以⻚为基本单位来管理存储空间的，我们的记录都会被分配到某个⻚中存储。⽽⼀个⻚的⼤⼩⼀般 是16KB，也就是16384字节，⽽⼀个VARCHAR(M)类型的列就最多 可以存储65532个字节，这样就可能造成⼀个⻚存放不了⼀条记录的 尴尬情况。</p><ul><li>在Compact和Reduntant⾏格式中，对于占⽤存储空间⾮常⼤的列，在记录的真实数据处<code>只会存储该列的⼀部分数据，把剩余的数据分散存储在⼏个其他的⻚中，然后记录的真实数据处⽤20个字节存储指向这些⻚的地址</code>（当然这20个字节中还包括这些分散在其他⻚⾯中的数据的占⽤的字节数），从⽽可以找到剩余数据所在的⻚</li></ul><h3 id="dynamic和compressed行格式" tabindex="-1"><a class="header-anchor" href="#dynamic和compressed行格式" aria-hidden="true">#</a> Dynamic和Compressed行格式</h3><p>这两个行格式和Compact行格式差不多，只不过在处理行溢出时有点分歧。</p><p>他们不在记录的真实数据出存储字段真实数据的前768个字节，而是把所有字节都存储在其他页面中，<code>只在记录的真实数据处存储其他页面的地址</code></p><p>Compressed⾏格式和Dynamic不同的⼀点是，Compressed⾏格式<code>会采⽤压缩算法对⻚⾯进⾏压缩，以节省空间。</code></p><h3 id="数据页结构" tabindex="-1"><a class="header-anchor" href="#数据页结构" aria-hidden="true">#</a> 数据页结构</h3><p>数据页代表的这块<code>16KB</code>大小的存储空间可以被划分为多个部分，不同部分有不同的功能，各个部分如图所示：</p><figure><img src="'+u+'" alt="image-20230316145811418" tabindex="0" loading="lazy"><figcaption>image-20230316145811418</figcaption></figure><p>从图中可以看出，一个<code>InnoDB</code>数据页的存储空间大致被划分成了<code>7</code>个部分，有的部分占用的字节数是确定的，有的部分占用的字节数是不确定的。下边我们用表格的方式来大致描述一下这7个部分都存储一些啥内容（快速的瞅一眼就行了，后边会详细唠叨的）：</p><figure><img src="'+h+`" alt="image-20200208110718819" tabindex="0" loading="lazy"><figcaption>image-20200208110718819</figcaption></figure><blockquote><p>小贴士： 我们接下来并不打算按照页中各个部分的出现顺序来依次介绍它们，因为各个部分中会出现很多大家目前不理解的概念，这会打击各位读文章的信心与兴趣，希望各位能接受这种拍摄手法～</p></blockquote><ol><li><p>InnoDB为了不同的目的而设计了不同类型的页，我们把用于存放记录的页叫做<code>数据页</code>。</p></li><li><p>一个数据页可以被大致划分为7个部分，分别是</p><ul><li><code>File Header</code>，表示页的一些通用信息，占固定的38字节。</li><li><code>Page Header</code>，表示数据页专有的一些信息，占固定的56个字节。</li><li><code>Infimum + Supremum</code>，两个虚拟的伪记录，分别表示页中的最小和最大记录，占固定的<code>26</code>个字节。</li><li><code>User Records</code>：真实存储我们插入的记录的部分，大小不固定。</li><li><code>Free Space</code>：页中尚未使用的部分，大小不确定。</li><li><code>Page Directory</code>：页中的某些记录相对位置，也就是各个槽在页面中的地址偏移量，大小不固定，插入的记录越多，这个部分占用的空间越多。</li><li><code>File Trailer</code>：用于检验页是否完整的部分，占用固定的8个字节。</li></ul></li><li><p>每个记录的头信息中都有一个<code>next_record</code>属性，从而使页中的所有记录串联成一个<code>单链表</code>(会按照主键值从小到大的顺序)。</p><ol><li><code>next_record</code>指向记录头信息和真实数据之间的位置，这个位置向左读取记录头信息(这就是为什么要逆序存放)，向右读取真实数据。</li></ol></li><li><p><code>InnoDB</code>会为把页中的记录划分为若干个组，每个组的最后一个记录的地址偏移量作为一个<code>槽</code>，存放在<code>Page Directory</code>中，所以在一个页中根据主键查找记录是非常快的，分为两步：</p><ul><li><p>通过二分法确定该记录所在的槽。</p></li><li><p>通过记录的next_record属性遍历该槽所在的组中的各个记录。</p></li></ul></li><li><p>每个数据页的<code>File Header</code>部分都有上一个和下一个页的编号，所以所有的数据页会组成一个<code>双链表</code>。</p></li><li><p>为保证从内存中同步到磁盘的页的完整性，在页的首部和尾部都会存储页中数据的校验和和页面最后修改时对应的<code>LSN</code>值，如果首部和尾部的校验和和<code>LSN</code>值校验不成功的话，就说明同步过程出现了问题。</p></li><li><p>如果InnoDB中的一条记录被删除了之后，它不会被立即删除，而是在他的记录头信息中将删除标志位取反。如果当前数据页中存在多条被删除的记录，这些记录的next_record属性将会把这些被删除掉的记录组成一个垃圾链表，以备之后重用这部分存储空间。</p></li></ol><hr><h1 id="三、索引" tabindex="-1"><a class="header-anchor" href="#三、索引" aria-hidden="true">#</a> 三、索引</h1><h2 id="索引方法和索引类型的区分" tabindex="-1"><a class="header-anchor" href="#索引方法和索引类型的区分" aria-hidden="true">#</a> 索引方法和索引类型的区分</h2><p>在MySQL中<strong>索引方法</strong>：<code>Btree索引、Hash索引</code>。</p><ul><li>innodb、myisam都只支持Btree索引</li><li>Memory、heap引擎两个都支持。</li></ul><p>索引的类别<code>由建立索引的字段内容特性</code>来决定。</p><p>索引类型又可以分为：</p><ul><li>全文索引</li><li>普通索引</li><li>空间索引</li><li>唯一索引</li></ul><h2 id="_3-1-b-树索引" tabindex="-1"><a class="header-anchor" href="#_3-1-b-树索引" aria-hidden="true">#</a> 3.1 B + 树索引</h2><h3 id="没有索引的查找" tabindex="-1"><a class="header-anchor" href="#没有索引的查找" aria-hidden="true">#</a> 没有索引的查找</h3><p>本集的主题是<code>索引</code>，在正式介绍<code>索引</code>之前，我们需要了解一下没有索引的时候是怎么查找记录的。为了方便大家理解，我们下边先只唠叨搜索条件为对某个列精确匹配的情况，所谓精确匹配，就是搜索条件中用等于<code>=</code>连接起的表达式，比如这样：</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>SELECT [列名列表] FROM 表名 WHERE 列名 = xxx;

</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><h4 id="在一个页中的查找" tabindex="-1"><a class="header-anchor" href="#在一个页中的查找" aria-hidden="true">#</a> 在一个页中的查找</h4><p>假设目前表中的记录比较少，所有的记录都可以被存放到一个页中，在查找记录的时候可以根据搜索条件的不同分为两种情况：</p><ul><li><p>以主键为搜索条件</p><p>这个查找过程我们已经很熟悉了，可以在<code>页目录</code>中使用二分法快速定位到对应的槽，然后再遍历该槽对应分组中的记录即可快速找到指定的记录。</p></li><li><p>以其他列作为搜索条件</p><p>对非主键列的查找的过程可就不这么幸运了，因为在数据页中并没有对非主键列建立所谓的<code>页目录</code>，所以我们无法通过二分法快速定位相应的<code>槽</code>。这种情况下只能从<code>最小记录</code>开始依次遍历单链表中的每条记录，然后对比每条记录是不是符合搜索条件。很显然，这种查找的效率是非常低的。</p></li></ul><h4 id="在很多页中查找" tabindex="-1"><a class="header-anchor" href="#在很多页中查找" aria-hidden="true">#</a> 在很多页中查找</h4><p>大部分情况下我们表中存放的记录都是非常多的，需要好多的数据页来存储这些记录。在很多页中查找记录的话可以分为两个步骤：</p><ol><li>定位到记录所在的页。</li><li>从所在的页内中查找相应的记录。</li></ol><p>在没有索引的情况下，不论是根据主键列或者其他列的值进行查找，由于我们并不能快速的定位到记录所在的页，所以只能从第一个页沿着双向链表一直往下找，在每一个页中根据我们刚刚唠叨过的查找方式去查找指定的记录。因为要遍历所有的数据页，所以这种方式显然是超级耗时的，如果一个表有一亿条记录，使用这种方式去查找记录那要等到猴年马月才能等到查找结果。所以祖国和人民都在期盼一种能高效完成搜索的方法，<code>索引</code>同志就要亮相登台了。</p><h3 id="innodb中的索引方案" tabindex="-1"><a class="header-anchor" href="#innodb中的索引方案" aria-hidden="true">#</a> InnoDB中的索引方案</h3><h4 id="聚簇索引" tabindex="-1"><a class="header-anchor" href="#聚簇索引" aria-hidden="true">#</a> 聚簇索引</h4><ol><li>使用记录主键值的大小<strong>进行记录和页的排序</strong>，包含三个方面的含义： <ul><li>页内的记录时按照主键的大小顺序排成一个单向链表</li><li>各个存放用户记录的页也是根据页中用户记录的主键大小顺序排成一个双向链表。</li><li>存放目录项记录的页分为不同的层次，在同一个层次的页也是根据野种目录项记录的逐渐大小顺序排成一个双向链表。</li></ul></li><li>B+树的叶子节点存储的是完整的用户记录 <ul><li>所谓完整的用户记录，就是指这个记录中存储了所有列的值(包含隐藏列)。</li></ul></li></ol><p>我们把具有这两种特性的B+树成为聚簇索引，所有完整的⽤户记录 都存放在这个聚簇索引的叶⼦节点处。</p><p><strong>存储过程：</strong></p><ol><li>创建表之后默认会创建一个聚簇索引。</li><li>当为某个表创建一个B+树索引时，就会为这个表创建一个根节点页面。</li><li>随后向表中插入数据时，先把用户数据存储到这个根节点中。</li><li>当<code>根节点</code>中的可用空间用完时继续插入记录，此时会将<code>根节点</code>中的所有记录复制到一个新分配的页，比如<code>页a</code>中，然后对这个新页进行<code>页分裂</code>的操作，得到另一个新页，比如<code>页b</code>。这时新插入的记录根据键值（也就是聚簇索引中的主键值，二级索引中对应的索引列的值）的大小就会被分配到<code>页a</code>或者<code>页b</code>中，而<code>根节点</code>便升级为存储目录项记录的页。 <ol><li>⼀个B+树索引的根节点⾃诞⽣之 ⽇起，便不会再移动</li></ol></li></ol><p><strong>内节点中目录项记录的唯一：</strong></p><ol><li><p>对于二级索引的内节点的目录项记录的内容实际上是由三个部分构成的：</p><ul><li><p>索引列的值</p></li><li><p>主键值</p></li><li><p>页号</p></li></ul></li><li><p>一个页面最少存储2条记录</p></li></ol><p><strong>总结：</strong></p><ol><li><p>对于<code>InnoDB</code>存储引擎来说，在单个页中查找某条记录分为两种情况：</p><ul><li><p>以主键为搜索条件，可以使用<code>Page Directory</code>通过二分法快速定位相应的用户记录。</p></li><li><p>以其他列为搜索条件，需要按照记录组成的单链表依次遍历各条记录。</p></li></ul></li><li><p>没有索引的情况下，不论是以主键还是其他列作为搜索条件，只能沿着页的双链表从左到右依次遍历各个页。</p></li><li><p><code>InnoDB</code>存储引擎的索引是一棵<code>B+</code>树，完整的用户记录都存储在<code>B+</code>树第<code>0</code>层的叶子节点，其他层次的节点都属于<code>内节点</code>，<code>内节点</code>里存储的是<code>目录项记录</code>。<code>InnoDB</code>的索引分为两大种：</p><ul><li><p>聚簇索引</p><p>以主键值的大小为页和记录的排序规则，在叶子节点处存储的记录<strong>包含了表中所有的列。</strong></p></li><li><p>二级索引</p><p>以自定义的列的大小为页和记录的排序规则，在叶子节点处存储的记录内容是<code>列 + 主键</code>。</p></li></ul></li><li><p><code>MyISAM</code>存储引擎的数据和索引分开存储，这种存储引擎的索引全部都是<code>二级索引</code>，在叶子节点处存储的是<code>列 + 页号</code>。</p></li></ol><h3 id="myisam中的索引方案" tabindex="-1"><a class="header-anchor" href="#myisam中的索引方案" aria-hidden="true">#</a> MyISAM中的索引方案</h3><p><code>MyISAM</code>的索引方案虽然也使用树形结构，但是却将<strong>索引和数据分开存储</strong>：</p><ul><li>将表中的记录按照记录的插入顺序单独存储在一个文件中，称之为<code>数据文件</code>。这个文件并不划分为若干个数据页，有多少记录就往这个文件中塞多少记录就成了。我们可以<strong>通过行号而快速访问到一条记录。</strong></li><li>插入数据的时候并没有可以的按照主键大小进行排序，所以我们并不能在这些数据上使用<strong>二分法</strong>进行查找。</li><li>该存储模型会将索引信息另外存储到一个文件中，<mark>MyISAM会单独为表的主键创建一个索引</mark>，只不过在索引的叶子节点中存储的不是完整的用户记录，<strong>而是<code>主键值 + 行号</code>的组合</strong>。也就是先通过索引找到对应的行号，再通过行号去找对应的记录！</li><li>所有索引都是二级索引 <ul><li>所以在MyISAM引擎中，每次根据主键值进行查找时，都需要一次回表。</li></ul></li></ul><hr><h2 id="_3-2、hash索引" tabindex="-1"><a class="header-anchor" href="#_3-2、hash索引" aria-hidden="true">#</a> 3.2、Hash索引</h2><ul><li>Hash索引：Hash索引底层是哈希表，哈希表是一种以key-value存储数据的结构，所以多个数据在存储关系上是完全没有任何顺序关系的，所以，对于区间查询是无法直接通过索引查询的，就需要全表扫描。所以，哈希索引只适用于等值查询的场景。<strong>而B+ 树是一种多路平衡查询树</strong>，所以他的节点是天然有序的（左子节点小于父节点、父节点小于右子节点），所以对于范围查询的时候不需要做全表扫描</li><li>B+树：在B树的基础上，将<strong>非叶节点改造为不存储数据纯索引节点</strong>，进一步降低了树的高度；此外将<strong>叶节点使用指针连接成链表</strong>，范围查询更加高效。</li></ul><h3 id="索引的代价" tabindex="-1"><a class="header-anchor" href="#索引的代价" aria-hidden="true">#</a> 索引的代价</h3><ul><li>空间上的代价： <ul><li>每建立一个索引都要为它创立一棵B+树，每一棵<code>B+</code>树的每一个节点都是一个数据页，一个页默认会占用<code>16KB</code>的存储空间</li></ul></li><li>时间上的代价： <ul><li>增、删、改操作可能会对节点和记录的排序造成破坏，所以存储引擎需要额外的时间进行一些记录移位，页面分裂、页面回收啥的操作来维护好节点和记录的排序。</li></ul></li></ul><p>换句话说，B+树索引不是万能的，并不是所有的查询语句都能用到我们建立的索引。</p><h3 id="b-树索引适用的条件" tabindex="-1"><a class="header-anchor" href="#b-树索引适用的条件" aria-hidden="true">#</a> B+树索引适用的条件</h3><ul><li><p>全值匹配</p><ul><li><p>如果我们的搜索条件中的列和索引列⼀致的话，这种情况就称为全值匹配。</p><div class="language-sql line-numbers-mode" data-ext="sql"><pre class="language-sql"><code><span class="token keyword">SELECT</span> <span class="token operator">*</span> <span class="token keyword">FROM</span> person_info <span class="token keyword">WHERE</span> birthday <span class="token operator">=</span> <span class="token string">&#39;1990-09-27&#39;</span> <span class="token operator">AND</span> phone_number <span class="token operator">=</span> <span class="token string">&#39;15123983239&#39;</span> <span class="token operator">AND</span> name<span class="token operator">=</span> <span class="token string">&#39;Ashburn&#39;</span><span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div></li><li><p>where后的搜索项顺序对查询结果没有影响(因为有查询优化器帮我们进行优化)。</p></li></ul></li><li><p>匹配<strong>联合索引</strong>左边的列</p><ul><li>例如：联合索引<code>a_b_c_d_e</code>，搜索条件中出现前缀的列才能使用到B+树索引。</li><li>话句话说，<code>where a = 1</code>, <code>where a = 1 and b = 1</code>, <code>where a = 1 and b = 1 and c == 1</code>等等都可以使用到B+树索引。</li></ul></li><li><p>匹配列前缀(like &quot;xxxx%&quot;)</p><ul><li>如果只给出后缀或者中间的某个字符串，MySQL就无法快速定位记录的位置。</li><li>如果只提供了后缀，那么可以在存储记录的时候就<code>逆序存储</code>。</li></ul></li><li><p>匹配范围值</p><ul><li><p>如果对多个列同时进行范围查找的话，只有对索引最左边的那个列进行范围查找的时候才能用到<code>B+</code>树索引.</p></li><li><div class="language-sql line-numbers-mode" data-ext="sql"><pre class="language-sql"><code><span class="token keyword">SELECT</span> <span class="token operator">*</span> <span class="token keyword">FROM</span> person_info <span class="token keyword">WHERE</span> name <span class="token operator">&gt;</span> <span class="token string">&#39;Asa&#39;</span> <span class="token operator">AND</span> name <span class="token operator">&lt;</span> <span class="token string">&#39;Barlow&#39;</span> <span class="token operator">AND</span> birthday <span class="token operator">&gt;</span> <span class="token string">&#39;1980-01-01&#39;</span><span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div></li><li><p>对于索引<code>idx_name_birthday</code>来说，只有name使用到了索引，通过name进⾏范围查找的记录中可能并不是按照birthday列进⾏排序的</p></li></ul></li><li><p>精确匹配某⼀列并范围匹配另外⼀列</p><ul><li>对于同⼀个联合索引来说，虽然对多个列都进⾏范围查找时只能⽤到 最左边那个索引列，但是如果左边的列是精确查找，则右边的列可以 进⾏范围查找</li></ul></li><li><p>用于排序</p><ul><li><p><code>order by a, b, c</code>需要使用到我们的索引列(即要有索引<code>idx_a_b_c</code>)，并且<code>a, b, c</code>的次序需要和索引列中的顺序一致</p></li><li><p>以下几种情况不能使用索引进行排序</p><ul><li><p>asc、desc混用</p></li><li><p>where字句中出现非排序使用到的索引列。</p><div class="language-sql line-numbers-mode" data-ext="sql"><pre class="language-sql"><code><span class="token keyword">SELECT</span> <span class="token operator">*</span> <span class="token keyword">FROM</span> person_info <span class="token keyword">WHERE</span> country <span class="token operator">=</span> <span class="token string">&#39;China&#39;</span> <span class="token keyword">ORDER</span> <span class="token keyword">BY</span> name <span class="token keyword">LIMIT</span> <span class="token number">10</span><span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div></li><li><p>排序列包含非同一个索引的列</p></li><li><p>排序列使用了复杂的表达式</p></li></ul></li></ul></li><li><p>用于分组</p><ul><li>分组列的顺序也需要和索引 列的顺序⼀致</li></ul></li></ul><h3 id="回表的代价" tabindex="-1"><a class="header-anchor" href="#回表的代价" aria-hidden="true">#</a> 回表的代价</h3><ul><li>访问二级索引使用<code>顺序I/O</code>，访问聚簇索引使用<code>随机I/O</code>。</li><li>访问二级索引效率会高一些，访问聚簇索引效率会低一些</li></ul><p>由于回表需要使用随机I/O，浪费大量的时间。</p><ol><li>需要回表的记录越多，使⽤⼆级索引的性能就越低。所以不一定会完全采用全表扫描的方式，也可能采用二级索引 + 回表的方式执行查询。这就是查询优化器做的工作。</li><li>添加了LIMIT 10的查询更容易让优化器采⽤⼆级索引 + 回表的⽅式进⾏查询。</li></ol><h3 id="带排序的分页查询优化-浅分页、深分页的优化" tabindex="-1"><a class="header-anchor" href="#带排序的分页查询优化-浅分页、深分页的优化" aria-hidden="true">#</a> 带排序的分页查询优化(浅分页、深分页的优化)</h3><p>PS：对于一张具有20万条数据的表，查询的时候需要进行大量的回表，效率极低，基本上不走索引。我们可以让他强制走索引来进行测试。</p><div class="language-sql line-numbers-mode" data-ext="sql"><pre class="language-sql"><code><span class="token keyword">select</span> xxx <span class="token keyword">from</span> tb <span class="token keyword">order</span> <span class="token keyword">by</span> xxx <span class="token keyword">desc</span> <span class="token keyword">limit</span> <span class="token number">80000</span><span class="token punctuation">,</span> <span class="token number">20</span><span class="token punctuation">;</span>
<span class="token keyword">select</span> xxx <span class="token keyword">from</span> tb <span class="token keyword">force</span> <span class="token keyword">index</span><span class="token punctuation">(</span>idx_xxx<span class="token punctuation">)</span> <span class="token keyword">order</span> <span class="token keyword">by</span> xxx <span class="token keyword">desc</span> <span class="token keyword">limit</span> <span class="token number">80000</span><span class="token punctuation">,</span> <span class="token number">20</span><span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><ol><li><p>浅分页可以给order by字段添加索引</p></li><li><p>深分页可以给order by和select字段添加联合索引。</p></li><li><p>如果需求经常变更，就把排序字段建立索引，可以通过手动回表，强制去走索引。</p></li><li><p>使用子查询查出所有符合条件的id，之后在查询，省去了回表的过程。</p></li><li><p>如果id是连续的，可以返回上次查询的最大记录，再往下limit</p><ol><li><p>分段获取，</p></li><li><div class="language-sql line-numbers-mode" data-ext="sql"><pre class="language-sql"><code> <span class="token keyword">select</span>  id<span class="token punctuation">,</span>name<span class="token punctuation">,</span>balance <span class="token keyword">FROM</span> account <span class="token keyword">where</span> id <span class="token operator">&gt;</span> <span class="token number">100000</span> <span class="token keyword">order</span> <span class="token keyword">by</span> id <span class="token keyword">limit</span> <span class="token number">10</span><span class="token punctuation">;</span>
 <span class="token keyword">select</span>  id<span class="token punctuation">,</span>name<span class="token punctuation">,</span>balance <span class="token keyword">FROM</span> account <span class="token keyword">where</span> id <span class="token operator">&gt;</span> <span class="token number">100000</span> <span class="token keyword">order</span> <span class="token keyword">by</span> id <span class="token keyword">limit</span> <span class="token number">10</span><span class="token punctuation">;</span>
 <span class="token keyword">select</span>  id<span class="token punctuation">,</span>name<span class="token punctuation">,</span>balance <span class="token keyword">FROM</span> account <span class="token keyword">where</span> id <span class="token operator">&gt;</span> <span class="token number">100000</span> <span class="token keyword">order</span> <span class="token keyword">by</span> id <span class="token keyword">limit</span> <span class="token number">10</span><span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div></li></ol><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>







</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div></li></ol><h3 id="覆盖索引" tabindex="-1"><a class="header-anchor" href="#覆盖索引" aria-hidden="true">#</a> 覆盖索引</h3><p>为了彻底告别<code>回表</code>操作带来的性能损耗，我们建议：<strong>最好在查询列表里只包含索引列</strong>。</p><p>因为我们只查询name, birthday, phone_number这三个索引列的 值，所以在通过idx_name_birthday_phone_number索引得到结 果后就不必到聚簇索引中再查找记录的剩余列，这样就省去了回表操作带来的性能损耗。</p><p>我们把这种只需要 ⽤到索引的查询⽅式称为索引覆盖。</p><p>当然，如果业务需要查询出索引以外的列，那还是以保证业务需求为重。但是我们很不鼓励用<code>*</code>号作为查询列表，最好把我们需要查询的列依次标明。</p><h3 id="如何挑索引" tabindex="-1"><a class="header-anchor" href="#如何挑索引" aria-hidden="true">#</a> 如何挑索引</h3><p>在使用索引时需要注意下边这些事项：</p><ul><li><p>只为用于搜索、排序或分组的列创建索引</p></li><li><p>为列的基数大的列创建索引</p><ul><li>列的基础指的是某一列中不重复数据的个数，列的基数越大，该列中的值越分散。好比在0、1布尔值上建立索引，重复值特别多，就会进行回表操作，性能损耗就更大了。</li></ul></li><li><p>索引列的类型尽量小(类型越小，比较越快，占的空间越小，一页可以寸的内容更多，进而加快读写效率)</p></li><li><p>可以只对字符串值的前缀建立索引(节约空间，加速比较)</p></li><li><p>只有索引列在比较表达式中单独出现才可以适用索引</p><ul><li><div class="language-sql line-numbers-mode" data-ext="sql"><pre class="language-sql"><code><span class="token number">1.</span> <span class="token keyword">WHERE</span> my_col <span class="token operator">*</span> <span class="token number">2</span> <span class="token operator">&lt;</span> <span class="token number">4</span>   <span class="token comment">// 会依次遍历所有的记录，计算这个表达式的值是不是⼩于4</span>
<span class="token number">2.</span> <span class="token keyword">WHERE</span> my_col <span class="token operator">&lt;</span> <span class="token number">4</span><span class="token operator">/</span><span class="token number">2</span>  <span class="token comment">//可以用索引</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div></li></ul></li><li><p>为了尽可能少的让<code>聚簇索引</code>发生页面分裂和记录移位的情况，建议让主键拥有<code>AUTO_INCREMENT</code>属性。</p><ul><li>那我们每插满⼀个数据⻚就换到下⼀个数据⻚继续插， ⽽如果我们插⼊的主键值忽⼤忽⼩的话，这就⽐较麻烦了，假设某个 数据⻚存储的记录已经满了，它存储的主键值在1~100之间：</li></ul></li><li><p>定位并删除表中的重复和冗余索引</p></li><li><p>尽量适用<code>覆盖索引</code>进行查询，避免<code>回表</code>带来的性能损耗。</p></li></ul><h2 id="视图" tabindex="-1"><a class="header-anchor" href="#视图" aria-hidden="true">#</a> 视图</h2><p><code>MySQL</code>中的视图其实是虚拟的表，也就是某个查询语句的一个别名而已，所以在存储<code>视图</code>的时候是不需要存储真实的数据的，只需要把它的结构存储起来就行了。</p><h2 id="表在文件系统上表示" tabindex="-1"><a class="header-anchor" href="#表在文件系统上表示" aria-hidden="true">#</a> 表在文件系统上表示</h2><p>表在文件系统上表示分两部分</p><ul><li><p>表结构的定义</p><p>不论是<code>InnoDB</code>还是<code>MyISAM</code>，都会在数据库子目录下创建一个和表名同名的<code>.frm</code>文件。</p></li><li><p>表中的数据</p><p>针对<code>InnoDB</code>和<code>MyISAM</code>对于表数据有不同的存储方式。</p></li></ul><h2 id="单表访问方法" tabindex="-1"><a class="header-anchor" href="#单表访问方法" aria-hidden="true">#</a> 单表访问方法</h2><figure><img src="`+g+`" alt="image-20230316210626456" tabindex="0" loading="lazy"><figcaption>image-20230316210626456</figcaption></figure><ul><li><p><code>index</code>: 采用遍历二级索引记录的执行方式称之为<code>index</code></p><ul><li><div class="language-sql line-numbers-mode" data-ext="sql"><pre class="language-sql"><code><span class="token keyword">SELECT</span> key_part1<span class="token punctuation">,</span> key_part2<span class="token punctuation">,</span> key_part3 <span class="token keyword">FROM</span> single_table <span class="token keyword">WHERE</span> key_part2 <span class="token operator">=</span> <span class="token string">&#39;abc&#39;</span><span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div></li><li><p>基本上都没用到索引，为什么不全表扫描，因为不需要回表并且二级索引记录比聚簇索引记录小得多。</p></li></ul></li><li><p><code>all</code>: 全表扫描</p></li><li><p><code>range</code>: 索引列<strong>需要匹配某个或某些范围的值</strong>。</p><ul><li><code>=</code>、<code>&lt;=&gt;</code>、<code>IN</code>、<code>NOT IN</code>、<code>IS NULL</code>、<code>IS NOT NULL</code>、<code>&gt;</code>、<code>&lt;</code>、<code>&gt;=</code>、<code>&lt;=</code>、<code>BETWEEN</code>、<code>!=</code>、<code>like</code></li></ul></li><li><p><code>ref</code>: 搜索条件为<strong>二级索引列与常数等值比较</strong>，采用二级索引来执行查询的访问方法称为：<code>ref</code></p></li><li><p><code>ref_or_null</code>: 当使用二级索引而不是全表扫描，和ref差不多，只不过会包含null值的行。</p></li><li><p><code>const</code>: 通过主键或者唯一二级索引列来定位一条记录(进⾏等值⽐较)的访问方法定义为：<code>const</code>，意思是常数级别的</p></li></ul><blockquote><p>特殊的：SELECT * FROM single_table WHERE key2 IS NULL;</p><p><u>普通的二级索引列</u>和<u>唯⼀⼆级索引列</u>并不限制NULL值的数量，所以上述语句可能访问到多条记录，也就是说上边这个语句不可以使⽤const访问⽅法来执⾏</p></blockquote><h2 id="索引合并" tabindex="-1"><a class="header-anchor" href="#索引合并" aria-hidden="true">#</a> 索引合并</h2><p>MySQL在⼀般情况下执⾏⼀个查询时最多只会⽤到单个⼆级索引，但不是还有特殊情况么，在这些特殊情况下也可能在⼀个 查询中使⽤到多个⼆级索引，设计MySQL的⼤叔把这种<strong>使⽤到多个索引来完成⼀次查询</strong>的执⾏⽅法称之为：<code>index merge</code></p><h3 id="intersection合并" tabindex="-1"><a class="header-anchor" href="#intersection合并" aria-hidden="true">#</a> intersection合并</h3><p>适⽤于使⽤不同索引的搜索条件之间使⽤AND连接起来的情况</p><p>这⾥是说某个查询可以使⽤多个⼆级索引，将从多个⼆级索引中查询到的结果取交集，之后才去回表。</p><p>这几种情况下才会使用索引合并：</p><ol><li>二级索引列时等值匹配，对于联合索引来说， 在联合索引中的每个列都必须等值匹配。 <ol><li>所有列都是等值匹配的话，这样记录才是按照主键排序的，后面合并的时候时间复杂度就是O(n)</li></ol></li><li>主键列可以时范围匹配</li></ol><h3 id="union合并" tabindex="-1"><a class="header-anchor" href="#union合并" aria-hidden="true">#</a> Union合并</h3><p>适⽤于使⽤不同索引的搜索条件之间使⽤OR连接起来的情况</p><ol><li><p>⼆级索引列是等值匹配的情况，对于联合索引来说， 在联合索引中的每个列都必须等值匹配，不能出现只出现匹配部分列的情况。</p></li><li><p>主键列可以是范围匹配</p></li><li><p>使⽤Intersection索引合并的搜索条件</p><div class="language-sql line-numbers-mode" data-ext="sql"><pre class="language-sql"><code><span class="token keyword">SELECT</span> <span class="token operator">*</span> <span class="token keyword">FROM</span> single_table <span class="token keyword">WHERE</span> key_part1 <span class="token operator">=</span> <span class="token string">&#39;a&#39;</span> <span class="token operator">AND</span> key_part2 <span class="token operator">=</span> <span class="token string">&#39;b&#39;</span> <span class="token operator">AND</span> key_part3 <span class="token operator">=</span> <span class="token string">&#39;c&#39;</span> <span class="token operator">OR</span> <span class="token punctuation">(</span>key1 <span class="token operator">=</span> <span class="token string">&#39;a&#39;</span> <span class="token operator">AND</span> key3 <span class="token operator">=</span> <span class="token string">&#39;b&#39;</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div></li></ol><h3 id="sort-union合并" tabindex="-1"><a class="header-anchor" href="#sort-union合并" aria-hidden="true">#</a> Sort-Union合并</h3><div class="language-sql line-numbers-mode" data-ext="sql"><pre class="language-sql"><code><span class="token keyword">SELECT</span> <span class="token operator">*</span> <span class="token keyword">FROM</span> single_table <span class="token keyword">WHERE</span> key1 <span class="token operator">&lt;</span> <span class="token string">&#39;a&#39;</span> <span class="token operator">OR</span> key3 <span class="token operator">&gt;</span> <span class="token string">&#39;z&#39;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><ol><li>先根据key1 &lt; &#39;a&#39;条件从idx_key1⼆级索引总获取记录， 并按照记录的主键值进⾏排序</li><li>再根据key3 &gt; &#39;z&#39;条件从idx_key3⼆级索引总获取记录， 并按照记录的主键值进⾏排序</li><li>因为上述的两个⼆级索引主键值都是排好序的，剩下的操作和 Union索引合并⽅式就⼀样了。</li></ol><p>我们把上述这种先按照⼆级索引记录的主键值进⾏排序，之后按 照Union索引合并⽅式执⾏的⽅式称之为Sort-Union索引合并，很 显然，这种Sort-Union索引合并⽐单纯的Union索引合并多了⼀步 对⼆级索引记录的主键值排序的过程。</p><h2 id="连接的原理" tabindex="-1"><a class="header-anchor" href="#连接的原理" aria-hidden="true">#</a> 连接的原理</h2><p>外连接和内连接的本质区别就是：对于外连接的驱动 表的记录来说，如果⽆法在被驱动表中找到匹配ON⼦句中的过滤条 件的记录，那么该记录仍然会被加⼊到结果集中，对应的被驱动表记 录的各个字段使⽤NULL值填充；⽽内连接的驱动表的记录如果⽆法 在被驱动表中找到匹配ON⼦句中的过滤条件的记录，那么该记录会 被舍弃</p><h4 id="_1-嵌套循环连接" tabindex="-1"><a class="header-anchor" href="#_1-嵌套循环连接" aria-hidden="true">#</a> 1. 嵌套循环连接</h4><ul><li><p>步骤1：选取驱动表，使用与驱动表相关的过滤条件，选取代价最低的单表访问方法来执行对驱动表的单表查询。</p></li><li><p>步骤2：对上一步骤中查询驱动表得到的结果集中每一条记录，都分别到被驱动表中查找匹配的记录。</p></li></ul><h4 id="_2-使用索引加快连接速度" tabindex="-1"><a class="header-anchor" href="#_2-使用索引加快连接速度" aria-hidden="true">#</a> 2. 使用索引加快连接速度</h4><h4 id="_3-基于快的嵌套循环连接" tabindex="-1"><a class="header-anchor" href="#_3-基于快的嵌套循环连接" aria-hidden="true">#</a> 3. 基于快的嵌套循环连接</h4><p>执行连接查询前申请的一块固定大小的内存，先把若干条驱动表结果集中的记录装在这个<code>join buffer</code>中，然后开始扫描被驱动表，每一条被驱动表的记录一次性和<code>join buffer</code>中的多条驱动表记录做匹配，因为匹配的过程都是在内存中完成的，所以这样<strong>可以显著减少被驱动表的<code>I/O</code>代价</strong>。</p><figure><img src="`+m+`" alt="image-20230316200916584" tabindex="0" loading="lazy"><figcaption>image-20230316200916584</figcaption></figure><h2 id="基于成本的优化" tabindex="-1"><a class="header-anchor" href="#基于成本的优化" aria-hidden="true">#</a> 基于成本的优化</h2><p>成本包括两部分：</p><ul><li>CPU成本</li><li>IO成本</li></ul><p>步骤大概是这样的：</p><ol><li>根据搜索条件，找出所有可能使用的索引。</li><li>计算全表扫描的代价(聚簇索引占用的页面数、该表中的记录数)</li><li>计算不同索引执行查询的代价</li><li>对比各种执行方案的代价，找出成本最低的一个</li></ol><p>表连接的成本计算</p><ol><li>MySQL中连接查询采⽤的是嵌套循环连接算法，驱动 表会被访问⼀次，被驱动表可能会被访问多次</li><li>包括单词查询驱动表的代价、多次查询被驱动表的代价</li></ol><h2 id="子查询在mysql中是如何执行的" tabindex="-1"><a class="header-anchor" href="#子查询在mysql中是如何执行的" aria-hidden="true">#</a> 子查询在MySQL中是如何执行的？</h2><h3 id="标量子查询、行子查询的执行方式" tabindex="-1"><a class="header-anchor" href="#标量子查询、行子查询的执行方式" aria-hidden="true">#</a> 标量⼦查询、⾏⼦查询的执⾏⽅式</h3><ol><li>对于包含不相关的标量⼦查询或者⾏⼦查询的查询语句来 说，MySQL会分别独⽴的执⾏外层查询和⼦查询，就当作两个单表 查询就好了</li></ol><div class="language-sql line-numbers-mode" data-ext="sql"><pre class="language-sql"><code><span class="token keyword">SELECT</span> <span class="token operator">*</span> <span class="token keyword">FROM</span> s1 <span class="token keyword">WHERE</span> key1 <span class="token operator">=</span> <span class="token punctuation">(</span><span class="token keyword">SELECT</span> common_field <span class="token keyword">FROM</span> s2 <span class="token keyword">WHERE</span> key3 <span class="token operator">=</span> <span class="token string">&#39;a&#39;</span> <span class="token keyword">LIMIT</span> <span class="token number">1</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><blockquote><p>先从外层查询中获取⼀条记录，本例中也就是先从s1表中获取 ⼀条记录。 然后从上⼀步骤中获取的那条记录中找出⼦查询中涉及到的 值，本例中就是从s1表中获取的那条记录中找出s1.key3列的 值，然后执⾏⼦查询。 最后根据⼦查询的查询结果来检测外层查询WHERE⼦句的条件 是否成⽴，如果成⽴，就把外层查询的那条记录加⼊到结果 集，否则就丢弃。 再次执⾏第⼀步，获取第⼆条外层查询中的记录，依次类推</p></blockquote><h3 id="in子查询优化" tabindex="-1"><a class="header-anchor" href="#in子查询优化" aria-hidden="true">#</a> IN⼦查询优化</h3><ol><li>如果in子查询的结果集较大的话，MySQL会将结果集放入临时表中，接着将临时表和外查询的表进行表连接查询。</li><li>对查询进行优化，直接进行表连接查询、semi-join连接查询(半连接是指只返回符合条件的数据行，而不返回所有匹配的行)</li></ol><h2 id="错误排查" tabindex="-1"><a class="header-anchor" href="#错误排查" aria-hidden="true">#</a> 错误排查</h2><h3 id="explain" tabindex="-1"><a class="header-anchor" href="#explain" aria-hidden="true">#</a> Explain</h3><ul><li><p><code>select_type</code>:</p><ul><li>查询语句中不包含<code>UNION</code>或者子查询的查询都算作是<code>SIMPLE</code>类型</li><li>对于包含<code>UNION</code>、<code>UNION ALL</code>或者子查询的大查询来说，它是由几个小查询组成的，其中最左边的那个查询的<code>select_type</code>值就是<code>PRIMARY</code></li></ul></li><li><p>type</p><ul><li>一条记录就代表着<code>MySQL</code>对某个表的执行查询时的访问方法，其中的<code>type</code>列就表明了访问单表的方法</li></ul></li><li><p>possible_keys和key</p><ul><li><strong>可能用到的索引</strong>和<strong>真实使用到的索引</strong></li></ul></li><li><p>key_len</p><ul><li>索引长度</li></ul></li><li><p>ref</p><ul><li>若使用索引列等值匹配的条件去执行查询时，ref展示与索引列匹配的值</li></ul></li><li><p>rows</p><ul><li>如果查询优化器决定使用全表扫描的方式对某个表执行查询时，执行计划的<code>rows</code>列就代表预计需要扫描的行数，如果使用索引来执行查询时，执行计划的<code>rows</code>列就代表预计扫描的索引记录行数。</li></ul></li><li><p>filtered</p></li></ul><h4 id="输出json格式的执行计划" tabindex="-1"><a class="header-anchor" href="#输出json格式的执行计划" aria-hidden="true">#</a> 输出Json格式的执行计划</h4><ul><li>在<code>EXPLAIN</code>单词和真正的查询语句中间加上<code>FORMAT=JSON</code>。</li></ul><h3 id="optimizer" tabindex="-1"><a class="header-anchor" href="#optimizer" aria-hidden="true">#</a> Optimizer</h3><p><code>ao p tei mei 妹崽 trace</code></p><p>使用MySQL5.6以及之前的数据库版本，只能通过explain语句来看到最后优化器决定使用的执行计划。</p><p>在<code>MySQL 5.6</code>以及之后的版本中，设计<code>MySQL</code>的大叔贴心的为这部分小伙伴提出了一个<code>optimizer trace</code>的功能，这个功能可以让我们方便的查看优化器生成执行计划的整个过程</p><p>查询优化器：</p><ul><li><strong>根据搜索条件找出所有可能的索引</strong></li><li><strong>计算全表扫描的代价</strong></li><li><strong>计算使用不同索引执行查询的代价</strong></li><li><strong>对比各种执行方案，找出成本最低的一种。</strong></li></ul><h3 id="show-profile" tabindex="-1"><a class="header-anchor" href="#show-profile" aria-hidden="true">#</a> show profile</h3><p>比explain更加细致的排查。</p><p>MySQL提供可以用来分析当前会话中语句执行的资源消耗情况，可以用于SQL的调优和测量。</p><p>默认情况下，参数处于关闭状态。</p><ul><li>开启</li></ul><figure><img src="`+b+'" alt="image-20200229174914201" tabindex="0" loading="lazy"><figcaption>image-20200229174914201</figcaption></figure><ul><li><img src="'+k+'" alt="image-20200229175056259" tabindex="0" loading="lazy"><figcaption>image-20200229175056259</figcaption></li><li>命令：<code>show profiles</code></li></ul><figure><img src="'+x+'" alt="image-20200229175259716" tabindex="0" loading="lazy"><figcaption>image-20200229175259716</figcaption></figure><p>看到一条sql的执行生命周期。</p><figure><img src="'+f+'" alt="image-20200229175535908" tabindex="0" loading="lazy"><figcaption>image-20200229175535908</figcaption></figure><h2 id="日志修复" tabindex="-1"><a class="header-anchor" href="#日志修复" aria-hidden="true">#</a> 日志修复</h2><h3 id="redo日志-innodb引擎层-持久性-掉电、故障恢复" tabindex="-1"><a class="header-anchor" href="#redo日志-innodb引擎层-持久性-掉电、故障恢复" aria-hidden="true">#</a> redo日志(innodb引擎层-持久性-掉电、故障恢复)</h3><ul><li><code>redo</code>日志占用的空间非常小</li><li><code>redo</code>日志是顺序写入磁盘的</li></ul><p>记录一下事务对数据库做了哪些修改。</p><p>redo log 是物理日志，记录了某个数据页做了什么修改，比如<strong>对 XXX 表空间中的 YYY 数据页 ZZZ 偏移量的地方做了AAA 更新</strong>，每当执行一个事务就会产生这样的一条或者多条物理日志。</p><p>在事务提交时，只要先将 redo log 持久化到磁盘即可，可以不需要等到将缓存在 Buffer Pool 里的脏页数据持久化到磁盘。</p><p>当系统崩溃时，虽然脏页数据没有持久化，但是 redo log 已经持久化，接着 MySQL 重启后，可以根据 redo log 的内容，将所有数据恢复到最新的状态。</p><figure><img src="'+_+'" alt="image-20230329141850272" tabindex="0" loading="lazy"><figcaption>image-20230329141850272</figcaption></figure><p>各个部分的详细释义如下：</p><ul><li><p><code>type</code>：该条<code>redo</code>日志的类型。</p><p>在<code>MySQL 5.7.21</code>这个版本中，设计<code>InnoDB</code>的大叔一共为<code>redo</code>日志设计了53种不同的类型，稍后会详细介绍不同类型的<code>redo</code>日志。</p></li><li><p><code>space ID</code>：表空间ID。</p></li><li><p><code>page number</code>：页号。</p></li><li><p><code>data</code>：该条<code>redo</code>日志的具体内容。</p></li></ul><blockquote><p>被修改 Undo 页面，需要记录对应 redo log 吗？</p></blockquote><p>需要的。</p><p>开启事务后，InnoDB 层更新记录前，首先要记录相应的 undo log，如果是更新操作，需要把被更新的列的旧值记下来，也就是要生成一条 undo log，undo log 会写入 Buffer Pool 中的 Undo 页面。</p><p>不过，<strong>在内存修改该 Undo 页面后，需要记录对应的 redo log</strong>。</p><blockquote><p>重做日志文件是记录数据修改之后的值，用于持久化到磁盘中。 redo log包括两部分： 一是内存中的日志缓冲(redo log buffer)，该部分日志是易失性的； 二是磁盘上的重做日志文件(redo log file)，该部分日志是持久的。由引擎层的InnoDB引擎实现,是物理日志,记录的是物理数据页修改的信息,比如“某个数据页上内容发生了哪些改动”。**当一条数据需要更新时,InnoDB会先将数据更新，然后记录redoLog 在内存中，然后找个时间将redoLog的操作执行到磁盘上的文件上。不管是否提交成功我都记录，你要是回滚了，那我连回滚的修改也记录。**它确保了事务的持久性。</p></blockquote><h3 id="bin-log日志-server层-数据备份-主从复制" tabindex="-1"><a class="header-anchor" href="#bin-log日志-server层-数据备份-主从复制" aria-hidden="true">#</a> bin log日志(server层-数据备份+主从复制)</h3><p>是<strong>Mysql服务层</strong>产生的日志，常用来进行数据恢复，数据库复制，常用的mysql主从复制架构，就是采用slave同步master的binlog实现的, 另外通过解析binlog能够实现mysql到其他数据源（如ElasticSearch)的数据复制。</p><h3 id="undo-log-innodb引擎层-原子性" tabindex="-1"><a class="header-anchor" href="#undo-log-innodb引擎层-原子性" aria-hidden="true">#</a> undo log(innodb引擎层-原子性)</h3><p>主要用于</p><ul><li><strong>事务中执行失败后，进行回滚</strong></li><li>MVCC中<strong>对于数据历史版本的查看</strong></li></ul><p>它保存了事务发生之前的数据的一个版本，用于回滚，同时可以提供多版本并发控制下的读（MVCC），也即非锁定读。</p><p>除了记录redo log外，当进行数据修改时还会记录undo log，undo log用于数据的撤回操作，它记录了修改的反向操作，比如，插入对应删除，修改对应修改为原来的数据，通过undo log可以实现事务回滚，并且可以根据undo log回溯到某个特定的版本的数据，实现MVCC。</p><h3 id="慢查询日志" tabindex="-1"><a class="header-anchor" href="#慢查询日志" aria-hidden="true">#</a> 慢查询日志</h3><p>默认情况下，MySQL数据库没有开启慢查询日志，需要我们手动来设置这个参数。</p><p>当然，如果不是调优需要的话，一般不建议启动该参数。</p><hr><h2 id="事务" tabindex="-1"><a class="header-anchor" href="#事务" aria-hidden="true">#</a> 事务</h2><ul><li><p>是数据库系统进行<strong>并发控制</strong>的基本单位</p></li><li><p>是数据库系统进行<strong>故障恢复</strong>的基本单位</p></li><li><p>是保持数据库<strong>状态一致性</strong>的基本单位</p><p>数据库系统是通过<mark>并发控制技术</mark>和<mark>日志恢复技术</mark>来对事务的ACID进行保证的。</p><figure><img src="https://img2018.cnblogs.com/blog/1422237/201811/1422237-20181122103402725-726722235.png" alt="img" tabindex="0" loading="lazy"><figcaption>img</figcaption></figure></li></ul><h4 id="事务的acid特性" tabindex="-1"><a class="header-anchor" href="#事务的acid特性" aria-hidden="true">#</a> 事务的ACID特性</h4><ul><li>原子性(undo log)</li><li>一致性()</li><li>隔离性(MVCC/锁)</li><li>持久性(redo log)</li></ul><h4 id="并发事务处理带来的问题" tabindex="-1"><a class="header-anchor" href="#并发事务处理带来的问题" aria-hidden="true">#</a> 并发事务处理带来的问题</h4><p>MySQL 服务端是允许多个客户端连接的，这意味着 MySQL 会出现同时处理多个事务的情况。</p><ul><li><p>更新丢失</p><ul><li>一个事务修改了另一个未提交事务修改过的数据，如果这时事务发生了回滚，那么未提交事务就白修改了，就出现了更新丢失的现象。</li></ul></li><li><p>脏读</p><ul><li>事务A读取到事务B<strong>已修改但尚未提交</strong>的数据。</li></ul></li><li><p>不可重复读</p><ul><li><img src="'+v+`" alt="image-20200308164036307" tabindex="0" loading="lazy"><figcaption>image-20200308164036307</figcaption></li><li>事务A读到了事务B已经提交的修改数据。</li></ul></li><li><p>幻读</p><ul><li>事务A读到了事务B提交的<strong>新增</strong>数据。</li></ul></li></ul><h4 id="事务隔离级别" tabindex="-1"><a class="header-anchor" href="#事务隔离级别" aria-hidden="true">#</a> 事务隔离级别</h4><p>我们上面介绍了几种并发事务执行过程中可能遇到的一些问题，这些问题也有轻重缓急之分，我们给这些问题按照严重性来排一下序：</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>更新丢失 &gt; 脏读 &gt; 不可重复读 &gt; 幻读
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>幻读、不可重复读、脏读都是数据库读一致性问题，必须由数据库提供一定的事务隔离机制来解决。</p><ul><li>未提交读</li><li>已提交读</li><li>可重复读(默认RR级别)</li><li>串行化</li></ul><figure><img src="`+y+`" alt="image-20200308161124874" tabindex="0" loading="lazy"><figcaption>image-20200308161124874</figcaption></figure><p>因为脏写这个问题太严重了，不论是哪种隔离级别，都不允许脏写的情况发生。</p><ol><li>查看当前事务的隔离级别 <code>show variables like &#39;tx_isolation&#39;</code></li><li>修改事务的隔离级别<code>SET [GLOBAL|SESSION] TRANSACTION ISOLATION LEVEL level;</code></li><li>MySQL虽然支持四种隔离级别，但与<code>SQL标准</code>中所规定的各级隔离级别允许发生的问题却又出入。 <ol><li>MySQL在<code>可重复读</code>级别下是可以禁止幻读问题的发生的。</li></ol></li></ol><p>开启事务</p><ul><li><code>begin;</code></li><li><code>start transaction [word]</code><ul><li>word: 有<code>read only、read write、with consistent snapshot</code></li><li>如果我们不显式指定事务的访问模式，那么该事务的访问模式 就是读写模式</li></ul></li></ul><p>提交事务</p><ul><li><code>commit;</code></li></ul><p>手动终止事务</p><ul><li><code>rollback;</code></li><li>ROLLBACK语句是我们程序员⼿动的去回滚事务 时才去使⽤的，如果事务在执⾏过程中遇到了某些错误⽽⽆法继续执 ⾏的话，事务⾃身会⾃动的回滚</li></ul><p>自动提交</p><ul><li><code>autocommit</code>， 默认值为on</li><li>默认情况下，如果我们不显式 的使⽤START TRANSACTION或者BEGIN语句开启⼀个事务，那么 每⼀条语句都算是⼀个独⽴的事务，这种特性称之为事务的⾃动提交。 <ul><li>显式的的使⽤START TRANSACTION或者BEGIN语句开启⼀个事务。 这样在本次事务提交或者回滚前会暂时关闭掉⾃动提交的功能。</li><li>把系统变量autocommit的值设置为OFF，就像这样： SET autocommit = OFF; 这样的话，我们写⼊的多条语句就算是属于同⼀个事务了，直到我们显式的写出COMMIT语句来把这个事务提交掉，或者显式的写出ROLLBACK语句来把这个事务回滚掉。</li></ul></li></ul><p>隐式提交</p><p>当我们使⽤START TRANSACTION或者BEGIN语句开启了⼀个事 务，或者把系统变量autocommit的值设置为OFF时，事务就不会进 ⾏⾃动提交，但是如果我们输⼊了某些语句之后就会悄悄的提交掉， 就像我们输⼊了COMMIT语句了⼀样，这种因为某些特殊的语句⽽导 致事务提交的情况称为隐式提交，这些会导致事务隐式提交的语句包 括：</p><ol><li>定义或修改数据库对象的数据定义语⾔</li><li>隐式使⽤或修改mysql数据库中的表</li><li>事务控制或关于锁定的语句 <ol><li>两个BEGIN</li></ol></li></ol><p>保存点</p><p>就是在事务对应的数据库语句中打⼏个 点，我们在调⽤ROLLBACK语句时可以指定会滚到哪个点，⽽不是回 到最初的原点。</p><p><code>savepoint xxx</code></p><p><code>rollback to xxx</code></p><hr><h2 id="锁" tabindex="-1"><a class="header-anchor" href="#锁" aria-hidden="true">#</a> 锁</h2><ul><li>有利有弊</li></ul><h3 id="对数据操作的类型" tabindex="-1"><a class="header-anchor" href="#对数据操作的类型" aria-hidden="true">#</a> 对数据操作的类型</h3><ul><li>读锁(共享锁) <ul><li>针对同一份数据，多个读操作可以同时进行而不会互相影响。</li></ul></li><li>写锁(共享锁) <ul><li>当前写操作没有完成前，他会阻断其他写锁和读锁。</li></ul></li></ul><h3 id="对数据操作的力度" tabindex="-1"><a class="header-anchor" href="#对数据操作的力度" aria-hidden="true">#</a> 对数据操作的力度</h3><h4 id="表锁-偏读" tabindex="-1"><a class="header-anchor" href="#表锁-偏读" aria-hidden="true">#</a> 表锁(偏读)</h4><ul><li><p>针对<code>MyISAM</code>存储引擎，开销小，加锁<strong>快</strong>。<strong>无</strong>死锁，锁定粒度**大****，发生锁冲突的概率最高，<strong>并发度最低。</strong></p></li><li><p>A对一个表加<strong>读锁</strong>之后</p><ul><li>A可以读这张表，不能修改这张表，也不能读其他表。</li><li>除A外的用户若想修改此表，会一直等待锁。</li></ul></li><li><div class="language-sql line-numbers-mode" data-ext="sql"><pre class="language-sql"><code><span class="token comment"># 加锁</span>
<span class="token keyword">lock</span> <span class="token keyword">table</span> 表名 <span class="token keyword">read</span><span class="token punctuation">(</span><span class="token keyword">write</span><span class="token punctuation">)</span> ；
<span class="token comment"># 解锁</span>
<span class="token keyword">unlock</span> <span class="token keyword">tables</span><span class="token punctuation">;</span>
<span class="token comment"># 查看哪些表被加锁</span>
<span class="token keyword">show</span> <span class="token keyword">open</span> <span class="token keyword">tables</span><span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div></li><li><p>A对一个表加<strong>写锁</strong>之后</p><ul><li>A可以读写此表，但不能读取其他表。</li><li>除A外的用户若想查看此表，会一直等待锁(更不用说修改了)。</li></ul></li><li><p>MyISAM在执行查询语句时，会自动给涉及的所有表加<strong>读锁</strong>，在执行增删改操作前，会自动给涉及的表加<strong>写锁</strong>。</p></li></ul><figure><img src="`+L+`" alt="image-20200308163355427" tabindex="0" loading="lazy"><figcaption>image-20200308163355427</figcaption></figure><p>此外，Myisam的读写锁调度是<strong>写优先</strong>，这也是MyISAM不适合做写为主表的引擎的原因。因为写锁后，其他线程不能做任何操作。</p><h4 id="行锁-偏写" tabindex="-1"><a class="header-anchor" href="#行锁-偏写" aria-hidden="true">#</a> 行锁(偏写)</h4><ul><li><p>偏向innodb存储引擎，开销<strong>小</strong>，加锁<strong>慢</strong>，<strong>会</strong>出现死锁。锁定粒度小，发生<strong>锁冲突的概率最低</strong>，并发度也最高。</p></li><li><p>索引失效之后，<strong>行锁变表锁。</strong></p><ul><li>varchar类型若不加单引号会导致索引失效。</li></ul><div class="language-sql line-numbers-mode" data-ext="sql"><pre class="language-sql"><code><span class="token keyword">begin</span><span class="token punctuation">;</span>
<span class="token comment"># 会一直阻塞其他session对该行的操作。</span>
<span class="token keyword">select</span> <span class="token operator">*</span> <span class="token keyword">from</span> 表名 <span class="token keyword">where</span> id  <span class="token operator">=</span>   x  <span class="token keyword">for</span> <span class="token keyword">update</span><span class="token punctuation">;</span>

</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div></li><li><p>如何分析行锁定</p><figure><img src="`+E+'" alt="image-20200308171239108" tabindex="0" loading="lazy"><figcaption>image-20200308171239108</figcaption></figure><p>​ 其中比较重要的是：</p><ul><li>innodb_row_locak_time_avg（平均等待时长）</li><li>innodb_row_lock_waits（等待总次数）</li><li>innodb_row_lock_time（等待总时长）</li></ul><p>当等待次数很高，并且每次等待时长也不小时。。。。。</p></li></ul><h6 id="优化建议" tabindex="-1"><a class="header-anchor" href="#优化建议" aria-hidden="true">#</a> 优化建议</h6><figure><img src="'+M+'" alt="image-20200308171629578" tabindex="0" loading="lazy"><figcaption>image-20200308171629578</figcaption></figure><h4 id="页锁" tabindex="-1"><a class="header-anchor" href="#页锁" aria-hidden="true">#</a> 页锁</h4><p>粒度介于表锁和行锁之间，性能也在之间。</p><h4 id="间隙锁" tabindex="-1"><a class="header-anchor" href="#间隙锁" aria-hidden="true">#</a> 间隙锁</h4><p>当我们用范围条件而不是相等条件时，并请求共享或排他锁时，InnoDB会给符和条件的已有数据记录的索引项进行加锁：对于锁值在条件范围内但不存在的记录，叫做间隙(GAP)</p><p>InnoDB会对间隙加锁，这种锁机制就是所谓的间隙锁。</p><p><strong>危害：</strong></p><p>因为Query执行过程中，通过范围查找的话，<strong>他会锁定整个范围内所有的索引键值，即使这个键值不存在。</strong></p><h4 id="innodb锁" tabindex="-1"><a class="header-anchor" href="#innodb锁" aria-hidden="true">#</a> InnoDB锁</h4><p><strong>只有执行计划真正使用了索引，才能使用行锁</strong></p>',275),B={href:"https://link.zhihu.com/?target=http%3A//mp.weixin.qq.com/s/h4B84UmzAUJ81iBY_FXNOg",target:"_blank",rel:"noopener noreferrer"},T=e("li",null,"不论是使用主键索引、唯一索引或普通索引，InnoDB 都会使用行锁来对数据加锁。",-1),D=i('<h3 id="多版本并发控制-mvcc" tabindex="-1"><a class="header-anchor" href="#多版本并发控制-mvcc" aria-hidden="true">#</a> 多版本并发控制(MVCC)</h3><h4 id="引子" tabindex="-1"><a class="header-anchor" href="#引子" aria-hidden="true">#</a> 引子</h4><p>最早的数据库系统，只有读读之间可以并发，读写，写读，写写都要阻塞。引入多版本之后，只有写写之间相互阻塞，其他三种操作都可以并行，这样大幅度提高了InnoDB的并发度。</p><h4 id="简介" tabindex="-1"><a class="header-anchor" href="#简介" aria-hidden="true">#</a> 简介</h4><p>是一个<strong>行级锁</strong>的变种，他在<strong>普通读情况下避免了加锁操作</strong>，因此开销更低。</p><p>虽然实现不同，但通常都是实现非阻塞读，对于写操作只锁定必要的行。</p><p><strong>适用范围：</strong></p><p>MVCC<strong>只在 <code>READ COMMITTED</code> 和 <code>REPEATABLE READ</code> 两个隔离级别下工作</strong>。</p><blockquote><p>其他两个隔离级别够和MVCC不兼容, 因为 <code>READ UNCOMMITTED</code> 总是读取最新的数据行, 而不是符合当前事务版本的数据行。而 <code>SERIALIZABLE</code> 则会对所有读取的行都加锁。</p></blockquote><p>每个连接到数据库的读者，<strong>在某个瞬间看到的是数据库的一个快照</strong>。写者写操作造成的变化在写操作完成之前(或者数据库事务提交之前)对于其他读者来说是不可见的。</p><p>MVCC 提供了时点（point in time）一致性视图。MVCC 并发控制下的读事务一般使用<strong>时间戳或者事务 ID</strong>去标记当前读的数据库的状态（版本），读取这个版本的数据。读、写事务相互隔离，不需要加锁。<strong>读写并存的时候，写操作会根据目前数据库的状态，创建一个新版本，并发的读则依旧访问旧版本的数据。</strong></p><p>一句话总结就是：</p><blockquote><p>MVCC(<code>Multiversion concurrency control</code>) 就是 <strong>同一份数据临时保留多版本的一种方式，进而实现并发控制</strong></p></blockquote><h4 id="相关概念" tabindex="-1"><a class="header-anchor" href="#相关概念" aria-hidden="true">#</a> 相关概念</h4><h5 id="_1-undo-log" tabindex="-1"><a class="header-anchor" href="#_1-undo-log" aria-hidden="true">#</a> 1. undo-log</h5><ul><li><p>undo-log是InnoDB MVCC事务特性的重要组成部分，当我们对记录做了变更修改时就会产生undo记录，undo记录默认被记录到系统表空间中，但从5.6开始，也可以使用独立的undo表空间。</p></li><li><p>Undo记录中存储的是老版本数据，当一个旧的事务需要读取数据时，为了能读取到老版本的数据，需要顺着undo链找到满足其可见性的记录。当版本链很长时，通常可以认为这是个比较耗时的操作（例如bug#69812）。</p></li><li><p>大多数对数据的变更操作包括INSERT/DELETE/UPDATE，其中INSERT操作在事务提交前只对当前事务可见，因此产生的Undo日志可以在事务提交后直接删除（谁会对刚插入的数据有可见性需求呢！！），而对于UPDATE/DELETE则需要维护多版本信息，在InnoDB里，UPDATE和DELETE操作产生的Undo日志被归成一类，即update_undo。</p></li><li><p>update undo log : 事务对记录进行delete和update操作时产生的undo log, 不仅在事务回滚时需要, 一致性读也需要，所以不能随便删除，只有当数据库所使用的快照中不涉及该日志记录，对应的回滚日志才会被purge线程删除。</p></li></ul><h5 id="_2-innodb中添加了三个隐藏字段" tabindex="-1"><a class="header-anchor" href="#_2-innodb中添加了三个隐藏字段" aria-hidden="true">#</a> 2. Innodb中添加了三个隐藏字段</h5><figure><img src="'+n+`" alt="image-20200207141345626" tabindex="0" loading="lazy"><figcaption>image-20200207141345626</figcaption></figure><p>(详情到<strong>记录存储结构</strong>中查看)</p><ul><li><strong><code>事务ID</code>(<code>DB_TRX_ID</code>)字段</strong>: 用来标识最近一次对本行记录做修改(insert|update)的事务的标识符, 即最后一次修改(<strong>insert|update</strong>)本行记录的事务id。</li><li><strong><code>回滚指针</code>(<code>DB_ROLL_PTR</code>)字段</strong>: 指写入回滚段(rollback segment)的 <code>undo log</code> record 的地址(撤销日志记录记录)。</li></ul><h5 id="_3-可见性比较算法" tabindex="-1"><a class="header-anchor" href="#_3-可见性比较算法" aria-hidden="true">#</a> 3. 可见性比较算法</h5><p>设要读取的行的最后提交事务id(即当前数据行的稳定事务id)为 <code>trx_id_current</code> 当前新开事务id为 <code>new_id</code> 当前新开事务创建的快照<code>read view</code> 中最早的事务id为<code>up_limit_id</code>, 最迟的事务id为<code>low_limit_id</code>(注意这个low_limit_id=未开启的事务id=当前最大事务id+1) 比较:</p><ul><li>1.<code>trx_id_current &lt; up_limit_id</code>, 这种情况比较好理解, 表示, 新事务在读取该行记录时, 该行记录的稳定事务ID是小于, 系统当前所有活跃的事务, 所以当前行稳定数据对新事务可见, 跳到步骤5.</li><li>2.<code>trx_id_current &gt;= trx_id_last</code>, 这种情况也比较好理解, 表示, 该行记录的稳定事务id是在本次新事务创建之后才开启的, 但是却在本次新事务执行第二个select前就commit了，所以该行记录的当前值不可见, 跳到步骤4。</li><li>3.<code>trx_id_current &lt;= trx_id_current &lt;= trx_id_last</code>, 表示: 该行记录所在事务在本次新事务创建的时候处于活动状态，从up_limit_id到low_limit_id进行遍历，如果trx_id_current等于他们之中的某个事务id的话，那么不可见, 调到步骤4,否则表示可见。</li><li>4.从该行记录的 DB_ROLL_PTR 指针所指向的回滚段中取出最新的undo-log的版本号, 将它赋值该 <code>trx_id_current</code>，然后跳到步骤1重新开始判断。</li><li>5.将该可见行的值返回。</li></ul><h4 id="实现方式" tabindex="-1"><a class="header-anchor" href="#实现方式" aria-hidden="true">#</a> 实现方式</h4><h5 id="乐观并发控制" tabindex="-1"><a class="header-anchor" href="#乐观并发控制" aria-hidden="true">#</a> 乐观并发控制</h5><h5 id="悲观并发控制" tabindex="-1"><a class="header-anchor" href="#悲观并发控制" aria-hidden="true">#</a> 悲观并发控制</h5><h4 id="readview" tabindex="-1"><a class="header-anchor" href="#readview" aria-hidden="true">#</a> ReadView</h4><p>已提交读和可重复读的区别就在于她们<strong>生成的ReadView的策略不同</strong>。</p><p>​</p><ul><li><strong>一致性读(就是读取快照)</strong><ul><li><code>select * from table</code></li></ul></li><li><strong>当前读(就是读取实际的持久化的数据)</strong><ul><li>特殊的读操作，插入、更新、删除操作，属于当前读，处理的都是当前的数据，需要加锁。</li><li></li></ul></li></ul><h4 id="当前读和快照读" tabindex="-1"><a class="header-anchor" href="#当前读和快照读" aria-hidden="true">#</a> 当前读和快照读</h4><ul><li>1.MySQL的InnoDB存储引擎默认事务隔离级别是RR(可重复读), 是通过 &quot;行排他锁+MVCC&quot; 一起实现的, 不仅可以保证可重复读, 还可以<strong>部分</strong>防止幻读, 而非完全防止。</li><li></li></ul><hr><h2 id="sql优化" tabindex="-1"><a class="header-anchor" href="#sql优化" aria-hidden="true">#</a> SQL优化</h2><ul><li><p><strong>对查询进行优化</strong>，避免全表扫描，首先应考虑在where以及<strong>order by涉及的列上建立索引</strong>。</p><ul><li>建立组合索引的时，将区分度高的字段放在前面</li></ul></li><li><p>**尽量使用自增主键，**数据行写入可以提高插入性能，避免数据页分裂和行记录移位，减少碎片空间提高空间和内存。</p><ul><li>主键进行使用较短的数据类型，因为所有索引都会使用到主键</li></ul></li><li><p>**禁止使用外键，**外键会导致表与表之间耦合，update与delete操作都会涉及相关联的表，十分影响sql 的性能，甚至会造成死锁。高并发情况下容易造成数据库性能，大数据高并发业务场景数据库使用以性能优先</p></li><li><p>经常用查询的字符字段应建立前缀字段，使用前缀索引可以只对字段的前几个字符进行索引，从而节约索引空间，提高索引效率。</p></li><li><p>应尽量便面在where子句中使用!=或&lt;&gt;操作符，有可能会导致引擎放弃使用索引。</p></li><li><p>大语句拆小语句，减少锁时间； 一条大sql可以堵死整个库</p></li><li><p>内连接的驱动表和被驱动表的位置可以互相替换。</p></li><li><p>小表驱动大表</p></li><li><p>而<code>左（外）连接</code>和<code>右（外）连接</code>的驱动表和被驱动表是固定的。</p></li><li><p>正因为<code>[NOT] IN/ANY/SOME/ALL</code>子查询不支持<code>LIMIT</code>语句，所以子查询中的这些语句也就是多余的了：</p></li></ul><h2 id="_1-宏观" tabindex="-1"><a class="header-anchor" href="#_1-宏观" aria-hidden="true">#</a> 1 宏观</h2><h3 id="_1-1-数据库锁" tabindex="-1"><a class="header-anchor" href="#_1-1-数据库锁" aria-hidden="true">#</a> 1.1 数据库锁</h3><ul><li>数据库锁适用于集群</li><li>粒度小，更方便控制</li></ul><h3 id="_1-2-代码锁" tabindex="-1"><a class="header-anchor" href="#_1-2-代码锁" aria-hidden="true">#</a> 1.2 代码锁</h3><ul><li>需要复杂的处理，才能作用于集群</li><li>粒度大</li></ul><h2 id="_2-微观" tabindex="-1"><a class="header-anchor" href="#_2-微观" aria-hidden="true">#</a> 2 微观</h2><h3 id="_2-1-行锁-表锁" tabindex="-1"><a class="header-anchor" href="#_2-1-行锁-表锁" aria-hidden="true">#</a> 2.1 行锁 &amp; 表锁</h3><h4 id="_2-1-1-说明" tabindex="-1"><a class="header-anchor" href="#_2-1-1-说明" aria-hidden="true">#</a> 2.1.1 说明</h4><p><strong>只有「明确」指定主键，才会执行锁，否则将会执行表锁</strong></p><h4 id="_2-1-2-示例" tabindex="-1"><a class="header-anchor" href="#_2-1-2-示例" aria-hidden="true">#</a> 2.1.2 示例</h4><p>假设有个表 products ，字段id、name、type，id是主键。</p><ul><li><p>无锁</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code># 明确指定主键，但不存在该主键的值(没有数据，当然不会有锁)
SELECT * FROM products WHERE id=-1 FOR UPDATE;
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div></li><li><p>行锁</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code># 明确指定主键
SELECT * FROM products WHERE id=3 FOR UPDATE;
SELECT * FROM products WHERE id=3 AND type=1 FOR UPDATE;
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div></li><li><p>表锁</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code># 主键不明确
SELECT * FROM products WHERE name=&#39;Mouse&#39; FOR UPDATE;
SELECT * FROM products WHERE id&lt;&gt;&#39;3&#39; FOR UPDATE;
SELECT * FROM products WHERE id LIKE &#39;3&#39; FOR UPDATE;
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div></li></ul><h4 id="_2-1-3-注意" tabindex="-1"><a class="header-anchor" href="#_2-1-3-注意" aria-hidden="true">#</a> 2.1.3 注意</h4><ul><li>要测试锁定的状况，可以利用 MySQL 的 Command Mode ，开二个视窗来做测试。</li><li>MyAsim 只支持表级锁，InnerDB支持行级锁添加了（行级锁、表级锁）锁的数据不能被其它事务再锁定，也不被其它事务修改（修改、删除） 。是表级锁时，不管是否查询到记录，都会锁定表。</li></ul><h3 id="_2-2-行锁算法" tabindex="-1"><a class="header-anchor" href="#_2-2-行锁算法" aria-hidden="true">#</a> 2.2 行锁算法</h3><h4 id="_2-2-1-record-lock-普通行锁" tabindex="-1"><a class="header-anchor" href="#_2-2-1-record-lock-普通行锁" aria-hidden="true">#</a> 2.2.1 Record Lock（普通行锁）</h4><ul><li>对于键值在条件范围内，且存在的记录，使用&quot; Record Lock &quot;，即普通的行锁机制；</li></ul><h4 id="_2-2-2-gap-lock-间隙锁" tabindex="-1"><a class="header-anchor" href="#_2-2-2-gap-lock-间隙锁" aria-hidden="true">#</a> 2.2.2 Gap Lock（间隙锁）</h4><ul><li>对于键值在条件范围内但并不存在的记录，叫做&quot; 间隙（GAP) &quot;，InnoDB会对这个“间隙”加锁，这种锁机制就是所谓的&quot; Gap Lock &quot;(间隙锁)；</li></ul><h4 id="_2-2-3-next-key-lock-行-间隙" tabindex="-1"><a class="header-anchor" href="#_2-2-3-next-key-lock-行-间隙" aria-hidden="true">#</a> 2.2.3 Next-Key Lock（行 &amp; 间隙）</h4><ul><li><p>对于存在于不存在的数据同时加锁，则称为&quot; Next-Key Lock &quot;；</p></li><li><p><strong>Next-Key Lock包含Record Lock和Gap Lock；</strong></p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code># 假如user表中只有101条记录，empid的值是1,2,...,100,101
# 范围条件的检索，会对值为101的记录加锁，也会对大于101（不存在）加锁
# 由于两个锁同时存在，则此处为 Next-Key Lock
select * from  user where user_id &gt; 100 for update;
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div></li></ul><h3 id="_2-3-表锁算法" tabindex="-1"><a class="header-anchor" href="#_2-3-表锁算法" aria-hidden="true">#</a> 2.3 表锁算法</h3><h4 id="_2-3-1-意向锁" tabindex="-1"><a class="header-anchor" href="#_2-3-1-意向锁" aria-hidden="true">#</a> 2.3.1 意向锁</h4><ul><li>当一个事务带着表锁去访问一个被加了行锁的资源，那么，此时，这个行锁就会升级为意向锁，将表锁住。</li><li>常用的意向锁有：意向共享锁，意向排它锁，共享意向排它锁</li></ul><h4 id="_2-3-2-自增锁" tabindex="-1"><a class="header-anchor" href="#_2-3-2-自增锁" aria-hidden="true">#</a> 2.3.2 自增锁</h4><ul><li><p>事务插入自增类型的列时获取自增锁</p><blockquote><p>如果一个事务正在往表中插入自增记录，所有其他事务的插入必须等待</p></blockquote></li></ul><h2 id="_3-实现" tabindex="-1"><a class="header-anchor" href="#_3-实现" aria-hidden="true">#</a> 3 实现</h2><h3 id="_3-1-共享锁-排它锁" tabindex="-1"><a class="header-anchor" href="#_3-1-共享锁-排它锁" aria-hidden="true">#</a> 3.1 共享锁 &amp; 排它锁</h3><blockquote><p>行锁和表锁是锁粒度的概念，共享锁和排它锁是他们的具体实现</p></blockquote><h4 id="_3-1-1-共享锁-s-读锁" tabindex="-1"><a class="header-anchor" href="#_3-1-1-共享锁-s-读锁" aria-hidden="true">#</a> 3.1.1 共享锁（S）：读锁</h4><ul><li>允许一个事务去读一行，阻止其他事务获取该行的排它锁。</li><li>多事务时，只能加共享读锁，不能加排他写锁；单事务时，可以加任何锁。</li><li>一般理解为：能读，不能写。</li></ul><h4 id="_3-1-2-排它锁-x-写锁" tabindex="-1"><a class="header-anchor" href="#_3-1-2-排它锁-x-写锁" aria-hidden="true">#</a> 3.1.2 排它锁（X）：写锁</h4><ul><li>允许持有排它锁的事务读写数据，阻止其他事物获取该数据的共享锁和排它锁。</li><li>其他事务不能获取该数据的任何锁，直到排它锁持有者释放。</li><li>不能获取任何锁，不代表不能无锁读取。</li></ul><p><strong>注意</strong></p><ul><li>排它锁指的是，在某个事务获取数据的排它锁后，其他事务不能获取该数据的任何锁， <strong>并不代表其他事务不能无锁读取该数据。</strong><ul><li>无锁 <ul><li><strong>select ... from</strong></li></ul></li><li>共享锁 <ul><li><strong>select ... lock in share mode</strong></li></ul></li><li>排它锁 <ul><li><strong>update</strong></li><li><strong>delete</strong></li><li><strong>insert</strong></li><li><strong>select ... for update</strong></li></ul></li></ul></li><li>MySQL8.0 中，<code>使用 FOR SHARE 替代了 LOCK IN SHARE MODE</code>，但仍然支持 LOCK IN SHARE MODE； 虽然是等价的，但是 FOR SHARE 支持 NOWAIT 、 SKIP LOCKED 等，配合自旋，可以实现高效的等待队列。</li></ul><h3 id="_3-2-乐观锁-悲观锁" tabindex="-1"><a class="header-anchor" href="#_3-2-乐观锁-悲观锁" aria-hidden="true">#</a> 3.2 乐观锁 &amp; 悲观锁</h3><blockquote><p>不管是什么锁都需要增加，需加失败重试</p></blockquote><h4 id="_3-2-1-乐观锁" tabindex="-1"><a class="header-anchor" href="#_3-2-1-乐观锁" aria-hidden="true">#</a> 3.2.1 乐观锁</h4><ul><li><p>通过版本号来进行更新的操作属于乐观锁</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>update tab set name = &#39;xxx&#39; where id = 1 and version = xxx
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div></li></ul><h4 id="_3-2-2-悲观锁" tabindex="-1"><a class="header-anchor" href="#_3-2-2-悲观锁" aria-hidden="true">#</a> 3.2.2 悲观锁</h4><blockquote><p>共享锁 &amp; 排它锁都是悲观锁的具象实现</p></blockquote><ul><li>显示地控制行或表锁属于悲观锁</li></ul><h2 id="_4-原理" tabindex="-1"><a class="header-anchor" href="#_4-原理" aria-hidden="true">#</a> 4 原理</h2><p>牵扯到索引巴拉巴拉...</p><h2 id="主从复制" tabindex="-1"><a class="header-anchor" href="#主从复制" aria-hidden="true">#</a> 主从复制</h2><h3 id="复制的基本原理" tabindex="-1"><a class="header-anchor" href="#复制的基本原理" aria-hidden="true">#</a> 复制的基本原理</h3><p>slave会从master读取binlog来进行数据同步。</p><figure><img src="`+I+'" alt="image-20200308171911959" tabindex="0" loading="lazy"><figcaption>image-20200308171911959</figcaption></figure><figure><img src="'+S+'" alt="image-20200308171949464" tabindex="0" loading="lazy"><figcaption>image-20200308171949464</figcaption></figure><figure><img src="'+R+'" alt="image-20200308172143963" tabindex="0" loading="lazy"><figcaption>image-20200308172143963</figcaption></figure><figure><img src="'+w+'" alt="image-20200308172212179" tabindex="0" loading="lazy"><figcaption>image-20200308172212179</figcaption></figure><figure><img src="'+A+'" alt="image-20200308172235874" tabindex="0" loading="lazy"><figcaption>image-20200308172235874</figcaption></figure><h3 id="主从复制的延迟问题" tabindex="-1"><a class="header-anchor" href="#主从复制的延迟问题" aria-hidden="true">#</a> 主从复制的延迟问题</h3><p>日志：记录、传输、解析</p><p>异步复制：降低写的响应时间，可能导致数据不一致或丢失</p><p>半同步复制：写请求，写入主库后，日志至少被送到一个从库上，写才会返回。</p>',91);function O(N,U){const o=s("ExternalLinkIcon");return d(),r("div",null,[C,e("ul",null,[e("li",null,[a("即便在条件中使用了索引字段，但是否使用索引来检索数据是由 MySQL 通过判断不同执行计划的代价来决定的，如果 MySQL 认为全表扫描效率更高，比如对一些很小的表，它就不会使用索引，这种情况下 InnoDB 将使用表锁，而不是行锁。因此，在分析锁冲突时，别忘了检查 SQL 的执行计划（可以通过 explain 检查 SQL 的执行计划），以确认是否真正使用了索引。（更多阅读："),e("a",B,[a("MySQL索引总结"),c(o)]),a("）")]),T]),D])}const H=l(q,[["render",O],["__file","SQL.html.vue"]]);export{H as default};
